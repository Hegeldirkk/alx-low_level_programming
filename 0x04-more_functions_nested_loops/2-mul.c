#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies a and b two integers
 * @a: Int representing a integer
 * @b: Int representing b integer
 * Return: Always 0;
 */
int mul(int a, int b)
{
	return (a * b);
}
