#include "operation.h"

/**
 * mul - multiplication of 2 number
 * @a: first int var
 * @b: second int var
 *
 * Return: sum
 */

int mul(int a, int b)
{
	int sum = 0;

	sum = a * (b);
	return (sum);
}
